# 更新日志

本项目所有值得记录的变更。日期为本地时间。

格式:**现象 → 根因 → 处理**。因为这个项目几乎每个坑都是"看起来是 A,
实际是 B",把根因写下来比写"修复了 XX"有用得多。

---

## [未发布]

### 控制面板:启动前先查两个外部程序,并支持手动指定路径

**现象**
点面板上的 `⑥ 启动 TTS 服务` / `② 启动语音回复`,既没提示也没反应;
或者跑到一半报一堆 `ImportError` / 合成失败,看不出到底哪一步不对。

**根因**
`WeChatBot_WXAUTO_SE-3.28` 和 `GPT-SoVITS-v2pro-...` 两个目录的位置是**写死**在
代码里的(面板的 `PROJECT` / `TTS_DIR` 常量、`voice_bot.py` 的 `PROJECT`、
启动脚本里的 `cd /d %ROOT%..\GPT-SoVITS-...`)。用户把程序放在别处、
或者改了目录名,就只能自己去改源码 —— 而面板对此一无所知,照样启动。

**处理**
- 新增 `01_核心模块/部署路径.py`,作为"两个外部目录在哪"的**唯一真值来源**:
  优先级 `环境变量 > VoiceBot/部署路径.json > 自动探测 > 默认目录名`,
  并提供校验、三态判定、自动扫描。
- 控制面板底部新增 **部署路径** 区:`选择…`(浏览目录)、`检测`(校验并保存)、
  `重新自动检测`,下方实时显示 ✅/❌ 与具体原因(缺哪个文件)。
- 点按钮前先把关,并把两种失败**分开说**:
  - 机器上压根没有 → 「未部署 GPT-SoVITS」/「未部署 WeChatBot」,附下载与指定步骤;
  - 装了只是路径指错 → 「TTS 路径不正确」/「WeChatBot 路径不正确」,
    把检测到的可用目录直接给出来,可一键改用。
- `voice_bot.py` / `gptsovits_tts.py` / `启动.cmd` / `启动TTS服务.cmd` 全部改为读同一份配置,
  免得出现"面板里选了路径,程序还在用旧的"。跨进程传递用环境变量
  `WECHATBOT_DIR` / `GPT_SOVITS_DIR`(面板启动子进程时注入)。
- `部署路径.json` 存本机绝对路径,已加入 `.gitignore`。

**踩到的坑**
- `CORE/vendor_py` 是真目录但被 gitignore(312MB 依赖),新用户拿到的是
  `PROJECT/vendor_py` —— 所以"检测 WeChatBot"必须认 `vendor_py` 或 `bot.py`。
- WeChatBot 项目里有 `gptsovits_tts.py` / `session_guard.py` 的**同名旧副本**,
  靠 `import 部署路径` 有被覆盖的风险,改用 `importlib` + 显式文件路径加载。
- 启动脚本里用 `for /f` 捕获 Python 输出,在 Python 路径含空格时会失败,
  改为写临时文件再 `set /p` 读回。
- 面板布局:五排按钮每排多占十几像素,就会把底部新增的部署路径区
  **整块挤出窗口**(截图实测,肉眼可见地消失)。改成按屏幕逻辑尺寸自适应,
  并把部署路径区钉在底部、只让运行输出区伸缩。
- `SetProcessDPIAware()` 是**进程级**设置,必须在创建任何窗口之前调用。
  原代码在后台线程里调它(只为 `FindWindowW` 找微信),结果在缩放显示器上
  界面被放大错位、右侧截断。改为保持默认的 DPI 不感知,由系统统一缩放。
- 注意 Tk 报的屏幕尺寸是**缩放后**的逻辑值:2560x1600 的屏在 175% 缩放下
  只报 1463x914,按物理像素估高度会把窗口设计得超出屏幕。

---

## [1.0.0] — 2026-09-19 · 正式版

首个正式版:从"能发出去"到"稳定发出去"。

### 能做什么

让 AI 角色用 **GPT-SoVITS 克隆音色**,在微信里发出**真正的语音条**。
全程走微信官方语音条通道 —— 无 Hook、无协议逆向、无进程内存改写。

### 正式版包含的能力

| 能力 | 说明 |
|---|---|
| 语音条发送 | 点语音条按钮 → 播到虚拟声卡 → 微信"听到" → 点发送 |
| 角色配置 Web UI | 浏览器里增删改角色与 prompt(127.0.0.1:5100) |
| 控制面板 | 环境自检 / 启动 / 停止 / 配置 / 留存管理 |
| 配置面板 | AI 模型与 Key、TTS 音色、参考音频、留存限额 |
| 音频留存 | 三重限额(条数/总大小/天数)自动清理 |
| 运行日志 | 每轮写入 `05_文档/运行日志/`,失败标明卡在第几步 |
| 首次运行引导 | 缺 `voice_bot.py` 时自动从模板生成 |
| 诊断模式 | `--debug` 打开逐步截图,失败自动存 `_fail_*.png` |

---

### 开发过程中修掉的关键问题

下面每一条都是实测踩出来的,按时间顺序。

#### 1. 空语音条(发出去了但没声音)

- **现象**:语音条正常显示,点开是空的。
- **根因**:诊断录音(`DIAG_RECORD`)和微信**同时打开了同一个录音端点**
  (VoiceMeeter Output)。两者抢占,微信取到的是静音。
- **处理**:默认关闭诊断录音;发送前校验音频峰值,过低直接拒发
  (`CABLE_SILENT_PEAK`),并做首尾静音修剪 + 音量归一化。

#### 2. 音色不像目标角色

- **现象**:合成出来不像。
- **根因**:参考音频的 `prompt_text` 为空会导致音色严重失真;
  参考音频超出 3~10 秒会被服务端直接拒绝。
- **处理**:`config_tool.py` 会校验参考音频时长并拦截不合规的文件
  (超出范围提示"参考音频在3~10秒范围外,请更换!"),
  同时在 `reference_config.json` 里记录 `prompt_text`。

#### 3. 改了代码却不生效

- **现象**:改了模块但运行结果没变。
- **根因**:项目目录下存在同名副本,**先于**新代码被 import。
- **处理**:修正 `sys.path` 插入顺序;两份副本改为字节同步。

#### 4. 会话被误关 / 会话框空白

- **现象**:本已打开的会话被点了一下反而关闭,输入框变空。
- **根因**:微信里点已打开的会话会 **toggle 关闭**;而"绿色高亮"只代表
  "最近有消息",**不代表会话已打开**,据此判断必然误判。
- **处理**:改用硬判据 —— 标题区灰阶极差(有会话 225 / 空白 0),
  OCR 读出标题一致就**跳过点击**。

#### 5. 行号漂移

- **现象**:`row` 过一段时间就指错人。
- **根因**:`--list` 的顺序来自**数据库**(按最后消息时间),而界面顺序是
  **置顶 + 时间**,两者不一致且随时间变化。
- **处理**:发送前用 OCR 核对标题,不符即中止;新增逐行探测自动纠正;
  文档强烈建议把目标会话**置顶**。

#### 6. 传文件 vs 传语音(UI 差异)

- **现象**:以为是同一个按钮。
- **根因**:**语音转文字的麦克风图标 ≠ 语音条按钮**。语音条按钮是发送键
  左边那个圆形声波图标。
- **处理**:坐标改用**实测值**,并支持窗口尺寸等比换算
  (`BUTTONS_MEASURED` / `MEASURED_WINDOW`)。

#### 7. 点击落到被遮挡的窗口上 ← 用户报的"没有发送点击"

- **现象**:微信里"录了音",但发送键**一次都没被点到**,鼠标停在语音条
  按钮上不动;偶尔还会误触发别的程序。
- **根因**:点击是「屏幕绝对坐标 + SendInput」,**只有该坐标真的属于微信
  窗口时才有效**。微信被浏览器/控制面板/任何窗口盖住时,点击会落到
  **盖在上面的那个程序**上。
  实测:微信被盖住时,三个按钮坐标的 `WindowFromPoint` 全部指向遮挡窗口。
- **处理**:三层防护 ——
  ① 每次点击前用 `WindowFromPoint` 校验坐标是否属于微信,不属于就先抢回
     前台重试 3 次;
  ② 仍不行就**拒绝点击**并报错,绝不去点别的程序;
  ③ 开跑前做整体预检,被遮挡就提前退出说明原因。

#### 8. 补点语音条按钮会取消录音 ← 上面那条的"真正根因"

- **现象**:同上,但更隐蔽 —— 而且**时好时坏**。
- **根因**:微信"录音工具栏变绿"有 **1.0~2.3 秒延迟**(时快时慢)。
  旧代码固定等 `0.8` 秒就采样一次,正好卡在延迟窗口里 →
  误判"没进入录音" → **补点一次语音条按钮** → 而这一下把刚开始的录音
  **直接取消**。实测:
  ```
  第1次 click → 1.1 秒后  录音态 = True    (其实已经录上了)
  第2次 click → 0.55 秒后 录音态 = False   (被自己点没了)
  ```
- **处理**:三条规则 ——
  ① 绝不"等绿色出现再播放"(等它就是把静音塞进语音条开头);
  ② 改成**先固定等录音管线就绪 → 立刻播放 → 播放之后再校验**,
     校验给 2.5 秒观察窗口,不用单次采样;
  ③ **只有确认不在录音态**时才允许补点重试,所以补点永不误伤。

#### 9. 语音条首尾静音过长

- **现象**:6 秒的话显示成 11~12 秒。
- **根因**:微信的录音从"点语音条按钮"开始、到"点发送"结束,
  程序中间的每个等待/截图**都会被录进去**。
  其中最大一块是 WASAPI 打开 VoiceMeeter 播放端要 **~0.7 秒**。
- **处理**:默认**紧凑模式** ——
  · 在点录音按钮**之前**就把播放流开好(播放耗时 1.96s → 1.25s)
  · 轮询代替固定等待;去掉发送前冗余截图;发送循环不再每轮抢前台
  · 效果:5.72 秒音频显示 **7 秒**

---

### 已知限制

- 微信窗口必须**最大化**;所有坐标按最大化标定。
- 运行时会**抢占前台焦点**,不能同时用鼠标做别的事。
- 微信 4.1.x 聊天区自绘渲染,UIAutomation 架构性不可用,因此依赖坐标模拟。
- 语音条**不支持发给群聊**(未验证)。
- 微信会压缩语音(约 3.4 kHz 以上被切),音色还原有上限。

### 注意

- 自动化操作微信可能违反其用户协议,存在账号被限制的风险。
  请仅在**自己有权控制的账号**上使用,后果自负。
- `voice_bot.py` / `config.py` / `reference_config.json` 含个人信息,
  已被 `.gitignore` 忽略,**不会**进入仓库。
